//fancy array "name"+count via slack
var inventory = [];
var fillArray() = groceryApp {
    inventory["water"] = 100,
        inventory["soda"] = 100,
        inventory["detergent"] = 50,
        inventory["apple"] = 100,
        inventory["banana"] = 100,
        inventory["tomato"] = 100,
        inventory["bread"] = 50,
        inventory["butter"] = 50,
        inventory["milk"] = 50,
        inventory["chicken"] = 50;
 }
 alert(inventory);
 //Add inventory count
console.log(inventory);

    function ListProducts(ProductsArray)
    {
    for (var i = 0; i < ProductsArray.length; i++) 
    {
        console.log (ProductsArray[i]);
      }
 }
 inventory.push("Kiwi");
 console.log (inventory);

 function splitAndPrint(name){
    var splitArr = name.split("");
    var i;
    splitArr.forEach(function(item, index, array) {
    console.log(item);
  });
//fillArray(groceryApp);
//fillArray(inventory);

